package alternativa.tanks.library.bonuses 
{
	/**
	 * ...
	 * @author TLVK official
	 */
	public class BonusLoader 
	{
		/*
		  private var callback:Function;
      
		  private var processes:Object;
		  
		  public function BonusLoader(callback:Function)
		  {
			 this.processes = {};
			 super();
			 this.callback = callback;
		  }
		  
		  public function load(descriptor:BonusDescriptor, callback:Function) : void
		  {
			 var process:BonusLoaderProcess = this.getOrCreateProcess(descriptor);
			 process.addListener(callback);
		  }
		  
		  private function getOrCreateProcess(descriptor:BonusDescriptor) : BonusLoaderProcess
		  {
			 var process:BonusLoaderProcess = this.processes[descriptor.id];
			 if(process == null)
			 {
				process = new BonusLoaderProcess(descriptor,this.onBonusReady);
				this.processes[descriptor.id] = process;
			 }
			 return process;
		  }
		  
		  private function onBonusReady(process:BonusLoaderProcess) : void
		  {
			 //delete this.processes[process.getBonusId()];
			// this.callback(process.getBonusId(),process.getBonus());
			// process.notifyListeners();
		  }
		*/
	}

}